{
  "name": "sample",
  "version": "0.0.4",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "bundle": "ng build --aot --delete-output-path --output-hashing=none"
  },
  "private": true,
  "sideEffects": false,
  "dependencies": {
    "sharepoint-choice": "^20.2.5"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^20.1.0",
    "@angular/cli": "^20.1.0"
  }
}
