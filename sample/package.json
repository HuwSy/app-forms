{
  "name": "sample",
  "version": "0.0.6",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "bundle": "ng build --aot --delete-output-path --output-hashing=none",
    "version": "npx -y shx rm -rf node_modules package-lock.json && npm install sharepoint-choice && (npm audit fix || echo npm audit fix reported issues, continuing) && git add . && git commit -m \"Version bump\" && git push"
  },
  "dependencies": {
    "sharepoint-choice": "^22.0.0"
  },
  "devDependencies": {
    "@angular/build": "^22.0.0",
    "@angular/cli": "^22.0.0"
  }
}
