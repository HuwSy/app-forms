{
  "name": "sample",
  "version": "0.0.3",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "bundle": "ng build --aot --delete-output-path --output-hashing=none"
  },
  "private": true,
  "sideEffects": false,
  "dependencies": {
    "sharepoint-choice": "^7.1.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^19.2.1",
    "@angular/cli": "^19.2.1"
  }
}
